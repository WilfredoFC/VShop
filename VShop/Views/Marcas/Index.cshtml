@model IEnumerable<MarcaViewModel>
@{
    ViewData["Title"] = "Marcas";
    ViewData["ActivePage"] = "Marcas";
}

<div class="container-fluid">
    <div class="row mb-4">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center">
                <div>
                    <h1 class="h3 mb-0">
                        <i class="fas fa-folder me-2"></i>Marcas
                    </h1>
                    <p class="text-muted mb-0">Gestiona las marcas de tus productos</p>
                </div>
                <div>
                    <a asp-action="Create" class="btn btn-vshop">
                        <i class="fas fa-plus-circle me-2"></i>Nueva Marca
                    </a>
                </div>
            </div>
        </div>
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="fas fa-check-circle me-2"></i>
            @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="card vshop-card">
        <div class="card-header bg-light">
            <h5 class="mb-0"><i class="fas fa-list me-2"></i>Lista de Marcas</h5>
        </div>
        <div class="card-body p-0">
            @if (Model.Any())
            {
                <div class="table-responsive">
                    <table class="table table-hover mb-0">
                        <thead>
                            <tr>
                                <th>Nombre</th>
                                <th>Estado</th>
                                <th>Fecha Creación</th>
                                <th class="text-center">Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var marca in Model)
                            {
                                <tr>
                                    <td>
                                        <strong>@marca.Nombre</strong>
                                        @if (!string.IsNullOrEmpty(marca.Descripcion))
                                        {
                                            <br>
                                            <small class="text-muted">@marca.Descripcion</small>
                                        }
                                        
                                    </td>
                                    <td>
                                        @if (marca.EsActivo)
                                        {
                                            <span class="badge bg-success">Activa</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-secondary">Inactiva</span>
                                        }
                                    </td>
                                    <td>@marca.FechaCreacion.ToString("dd/MM/yyyy")</td>
                                    
                                    <td class="text-center">
                                        <div class="btn-group btn-group-sm" role="group">
                                            <a asp-action="Edit" asp-route-id="@marca.Id"
                                               class="btn btn-outline-primary" title="Editar">
                                                <i class="fas fa-edit"></i>
                                            </a>
                                            <button type="button" class="btn btn-outline-danger"
                                                    title="Eliminar" data-bs-toggle="modal"
                                                    data-bs-target="#deleteModal"
                                                    data-id="@marca.Id"
                                                    data-nombre="@marca.Nombre">
                                                <i class="fas fa-trash"></i>
                                            </button>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="text-center py-5">
                    <i class="fas fa-folder-open fa-3x text-muted mb-3"></i>
                    <h4 class="text-muted">No hay marcas creadas</h4>
                    <p class="text-muted mb-4">Comienza organizando tus productos en marcas</p>
                    <a asp-action="Create" class="btn btn-vshop">
                        <i class="fas fa-plus-circle me-2"></i>Crear primera marca
                    </a>
                </div>
            }
        </div>
    </div>
</div>

<!-- Modal de eliminación -->
<div class="modal fade" id="deleteModal" tabindex="-1" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-danger text-white">
                <h5 class="modal-title" id="deleteModalLabel">
                    <i class="fas fa-exclamation-triangle me-2"></i>Confirmar Eliminación
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p>¿Estás seguro de que deseas eliminar la marca <strong id="marcaNombreEliminar"></strong>?</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                <form id="formEliminar" method="post" asp-action="Delete">
                    <input type="hidden" id="marcaIdEliminar" name="id" />
                    <button type="submit" class="btn btn-danger">Eliminar Marca</button>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            // Modal de eliminación
            $('#deleteModal').on('show.bs.modal', function(event) {
                var button = $(event.relatedTarget);
                var id = button.data('id');
                var nombre = button.data('nombre');

                var modal = $(this);
                modal.find('#marcaNombreEliminar').text(nombre);
                modal.find('#marcaIdEliminar').val(id);
            });
        });
    </script>
}