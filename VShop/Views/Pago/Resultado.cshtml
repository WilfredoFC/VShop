@model VShop.Application.Dtos.Pedido.PedidoDto

@{
    ViewData["Title"] = "Resultado del Pago";
}

<div class="container mt-5">
    <div class="alert alert-warning" role="alert">
        <h4 class="alert-heading">
            <i class="fas fa-lock"></i> Sistema de Pagos Deshabilitado
        </h4>
        <p>
            El sistema de pagos está temporalmente deshabilitado. Esta página no está disponible en este momento.
        </p>
    </div>

    <div class="row justify-content-center mt-4">
        <div class="col-md-6">
            <div class="card">
                <div class="card-body text-center">
                    <a href="/Productos" class="btn btn-primary mt-3">
                        <i class="fas fa-arrow-left"></i> Volver al Catálogo
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
